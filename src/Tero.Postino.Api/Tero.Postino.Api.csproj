<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.OpenApi" />
    <PackageReference Include="RabbitMQ.Client" />
    <!-- Reemplaza el filtro de auth estático (X-Tero-Service-Id/Token) por JWT de servicio,
         mismo mecanismo que Auth/Appointments/Gateway. -->
    <PackageReference Include="Microsoft.AspNetCore.Authentication.JwtBearer" />
  </ItemGroup>

  <ItemGroup>
    <!-- La librería compartida llega por paquete, no por ruta: este repo tiene que compilar
         solo, fuera del repo que orquesta el sistema. -->
    <PackageReference Include="Tero.Contracts" />
    <PackageReference Include="Tero.Messaging" />
    <PackageReference Include="Tero.ServiceDefaults" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Tero.Postino.Application\Tero.Postino.Application.csproj" />
    <ProjectReference Include="..\Tero.Postino.Infrastructure\Tero.Postino.Infrastructure.csproj" />
  </ItemGroup>

</Project>
